# ДРАГМЕТ

Идл-игра про бомжа-аффинажника: копаешься в мусоре → собираешь электронику → растворяешь в кислоте → выпариваешь в печи → продаёшь золото скупщику → прокачиваешься → престиж → кузница с амулетами.

## Текущий статус

- **Стадия:** рабочий HTML-прототип. Заход 1 (критические баги) закрыт на 90%, осталось доделать подсветку.
- **Версия:** v0.9 (код сейва `ver: 22`).
- **Формат:** один файл `index.html` + две картинки в той же папке: `kuznec.png` (открытый кузнец), `kuznec-locked.png` (закрытый, тёмный размытый силуэт).
- **Где живёт:** GitHub Pages (`txnto-exe.github.io/dragmet`), открывается в Safari на iPhone, добавлен на домашний экран как PWA.
- **Тестируют:** друзья (обычные игроки, не программисты).

## Roadmap

1. ✅ **Этап 1 — фиксы.** Кузница до престижа, свайп амулетов, bloom, DEV-консоль.
2. ✅ **Этап 2 — визуал и удобство.** Оффлайн-модалка, инвентарь, аккордеоны, мини-игра.
3. ✅ **Этап 3 — контент кузницы.** Рецепты чипами, время крафта, «Два горна».
4. ✅ **Этап 4 — баланс престижа.** Порог `100k × 1.5^N`, потолок 50 уровней.
5. **Сейчас:** Заход 1 — критические баги из баг-репорта.
6. **Дальше:** Заход 2 — баланс (плавающая цена, конечность прокачек, куда девать очки).
7. **Потом:** Техдолг, пиксельный редизайн, обёртка в `.ipa` / `.apk`, убрать DEV-консоль.

## ГЛАВНЫЕ ПРАВИЛА РАБОТЫ С АВТОРОМ

**Эти правила железные. Нарушались уже много раз — не повторять.**

1. **Никакого кода без явной команды.** Пока автор не сказал «пиши» или «делай» — код не пишется, не предлагается, не показывается. Только обсуждение, анализ, вопросы.
2. **Только полные готовые файлы.** Никогда не давать куски кода. Никогда не говорить «найди там, замени тут, посмотри здесь». Только целиком `index.html`, который автор копирует и заменяет.
3. **Автор не программист.** Объяснять по-простому, без терминов. Если нужен термин — сразу расшифровывать.
4. **Без воды.** Отвечать коротко и по делу.
5. **Сначала слушать, потом предлагать.** Автор сам говорит что нужно и в каком порядке. Не лезть с непрошеными советами.
6. **Перед задачей — уточнять и проговаривать.** Если задача размытая — задать уточняющие вопросы, резюмировать «что именно делаем», дождаться «делай».
7. **Не делать всё сразу.** Большие задачи разбивать на этапы и создавать план. Начинать только после подтверждения.
8. **Если автор путается — попросить скрин.** Один скрин снимает десять уточняющих вопросов. Не гадать.
9. **Если фикс не работает — не просить автора «заменить две функции».** Автор не программист. Только целый файл.

## Технический стек

- **Чистый HTML + CSS + vanilla JS.** Никаких библиотек, фреймворков, npm, сборщиков.
- **Причина:** простота, скорость правок, открывается где угодно, легко превращается в нативное приложение через WebView.
- **Не менять этот принцип** без явной просьбы.

## Структура файла

Один файл `index.html` содержит:
- CSS (в `<style>`),
- HTML-разметку (экраны меню, игры),
- весь JS в одном IIFE (в `<script>`).

Плюс картинки в **той же папке**, что `index.html`:
- `kuznec.png` — открытый кузнец.
- `kuznec-locked.png` — закрытый кузнец (тёмный размытый силуэт).

## Экраны игры

- **Меню** (`menuScreen`): Продолжить / Новая игра.
- **Игра** (`gameScreen`): 5 вкладок.
  - **Кузнец** (`forge`) — открывается после первого престижа. До престижа на иконке — только 🔒 без подписи.
  - **Печь** (`fire`) — выпаривание раствора в слитки, скупщик, продажа.
  - **Копать** (`dig`) — основная вкладка, точки сбора, инвентарь. **Без вертикального скролла.**
  - **Кислота** (`acid`) — растворение лома.
  - **Прокачка** (`upg`) — все апгрейды + престиж. Две вкладки: Обычная / Престиж.
- **Оверлей оффлайн-дохода.**
- **Оверлей стоп-игры «Контроль цены».**
- **DEV-консоль** — по 5 быстрым тапам на надпись ДРАГМЕТ в шапке.

## Игровая петля

1. **Копать** — на локациях тратится «запас» точки, находит мусор.
2. **Кислота** — растворяет лом, часть золота теряется.
3. **Печь** — выпаривает раствор, получаются слитки.
4. **Скупщик** — продажа слитков через стоп-игру.
5. **Прокачка** — апгрейды + престиж.
6. **Кузница** (после первого престижа) — крафт амулетов из мусора.

## Сохранения

- **Два независимых слота:** `dragmet_save_a_v2` и `dragmet_save_b_v2`.
- Пишем по очереди в тот слот, у которого **меньше** timestamp.
- При загрузке берём тот, что свежее.
- **Старые ключи** удаляются при запуске.
- **Текущий `ver`: 22.** При загрузке старого сейва без `forgeCrafts` — конвертируется из старого `forgeCraft`. Поле `priceControl` больше не сохраняется.

## Игровые механики (важное)

### Автокопатели

- Покупаются за кэш, по одному на локацию: 800 / 2 500 / 6 000 / 15 000 / 35 000 / 80 000 / 180 000 / 260 000 / 400 000 ₽.
- Работают без траты стамины точек (так задумано, не баг).
- Не сбрасываются при престиже.
- Прокачки общие: «Скорость работяг» (+10% за уровень) и «Снижение штрафа» (−5% за уровень, максимум 10).
- Замедлены в 3 раза (`AUTO_SLOW_FACTOR = 3`).

### Оффлайн-доход

- Считается от купленных автокопателей.
- **Порог срабатывания — 5 минут.**
- `lastSeenTs` обновляется всегда.
- Лимит часов по умолчанию 1 час, до 10.
- Коэффициент оффлайна — база 0%, +5% за уровень, до 75%.
- **Модалка:** время, крупная сумма, случайная фраза (8 вариантов), кнопка «Забрать».

### Кузница

- **Открывается после первого престижа.**
- **До престижа:** `kuznec-locked.png` нормальной яркости, сверху плашка с 🔒 и надписью «ЗАКРЫТО». Анимации выключены. Заголовок «Кузница» и аккордеон в Прокачке спрятаны. В таббаре — только 🔒.
- **После престижа:** `kuznec.png`, все анимации включены, всё показывается.

**Анимации кузнеца (только когда открыта):**
- Дыхание картинки (4.2 сек цикл).
- Дым от сигареты: 7 частиц, точка `50% / 24%`.
- Свечение лампы: `91% / 58%`, цикл 2.4 сек.
- Тёплый пульс снизу.
- Светлячки: 5 точек вокруг лампы (`85–92%` / `46–52%`).

### Амулеты (10 штук)

- Один слот одновременно.
- Эффекты: Ночная смена, Второй шанс, Золотая жила, Чистюля, Печной демон, Расширитель, Щедрый скупщик, Мусорщик, Легендарный охотник, Терпеливый.
- Рецепты — чипами `🔌 Микросхемы 3/5`, красные если не хватает.
- **Удаление:** свайп влево → красная корзина → тап → растворяется, вернёт 40%. Без `confirm`.
- **При престиже:** амулеты остаются, срок в престижах −1.

### Крафт амулетов

- **Один горн** по умолчанию.
- **«Два горна»** — 25 000 ₽, в Прокачке → Кузница. Позволяет запустить два параллельно.
- **Нельзя запустить один и тот же амулет дважды** — проверяется `forgeCrafts`.
- Пока слоты заняты — кнопки «Занят».

### Прокачка кузницы

- **Быстрее куём** — −10% времени за уровень, макс 7.
- **Дешевле рецепты** — −5% ресурсов, макс 10.
- **Дольше живут** — +1 престиж / +25% времени за уровень, макс 5.
- **Два горна** — покупка один раз, 25 000 ₽.

### «Контроль цены» (стоп-игра)

- **Доступна с самого начала.**
- Кнопка «Продать» → бегунок, зелёная зона, тап → стоп.
- **До тапа** — диапазон цен (`800 – 4000 ₽/г`).
- **После тапа** — выпавшее число.
- Попал в зону → 4000 ₽/г, промазал → от 3900 до 800.
- **«Широкая зона»** — +1% ширины за уровень, макс 5. База 5 000 ₽ × 1.7^lv.

### Престиж

- **Порог:** `100 000 × 1.5^уровень`.
  - 0→1: 100 000 ₽
  - 1→2: 150 000 ₽
  - 2→3: 225 000 ₽
  - 5→6: ~1,14 млн ₽
  - 10→11: ~8,6 млн ₽
  - 49→50: ~42,5 трлн ₽
- **Награда:** `10 × 2.5^(уровень-1)`.
- **Максимум 50 уровней.** После 50 кнопка блокируется, показывается `★ ФИНАЛ — престиж 50`.
- **Что сбрасывается:** деньги, слитки, лом, раствор, обычные прокачки, локации, склад.
- **Что остаётся:** автокопатели, амулеты, очки престижа, апгрейды престижа.

### Звук

- Единый уровень `SND_VOL × VOL_MUL = 0.07 × 14`.
- Тон кислоты 600 → 1800 Гц.
- Печь — потрескивание 800–1800 Гц.
- **`tone()` и `slide()`** имеют мёртвый параметр `vol` — оставлен, потому что удаление требует переписать ~30 вызовов. Не баг.

### Тост о находке

- **Внутри вкладки «Копать»** — плавающий, встаёт на строку «N/M» между стрелками локаций.
- **В других вкладках** — по центру панели.
- Цвет по тиру предмета.

### Подсветка найденного

**⚠️ СЛОМАНО.** Функция `flashInvItem()` навешивает класс `.flash` на DOM-элемент, но `renderInventory()` пересобирает инвентарь каждый кадр через `innerHTML` — класс слетает раньше, чем игрок его увидит. В последнем заходе предложено решение через `window._flashInvKey` / `window._flashInvUntil`, но автор ещё не тестировал. **Незакрытый баг.**

### Управление касаниями

- **«Копать» и «Продать»** — моментально (30 мс). Держишь → турбо-палец. Сдвинул >12px → не копает и не скроллит.
- **Всё остальное** — срабатывает при отпускании, если палец не сдвинулся.
- **Тап «Порыться» в кулдауне** — лёгкий звук + вибро (отклик есть, действие не запускается).

### Скроллбары

Скрыты везде.

### Вкладка «Копать» — особенности

- **Без вертикального скролла.** `100dvh`.
- **Точки сбора:** по 3 на страницу, страниц 3 (9 локаций). Стрелки ← →, плавный слайд 0.32 сек.
- **Инвентарь:** всегда открыт, 16 предметов, **4×4**, ровный квадрат.

## Локации (9 штук)

| Ключ | Название | Цена |
|---|---|---|
| yard | Баки во дворе | 0 |
| construction | Заброшенная стройка | 650 |
| radiozavod | Свалка радиозавода | 2 600 |
| garages | Гаражный кооператив | 6 500 |
| workshop | Ремонтная мастерская | 14 000 |
| ats | Заброшенная АТС | 32 000 |
| scrapyard | Приёмка металлолома | 75 000 |
| datacenter | Заброшенный дата-центр | 130 000 |
| institute | Институтский склад | 180 000 |

## Предметы (16 штук)

| Иконка | Название | Au (г) |
|---|---|---|
| 🔘 | Резисторы | 0.005 |
| 🔋 | Конденсаторы | 0.008 |
| 🔻 | Транзисторы | 0.015 |
| 🔌 | Микросхемы | 0.02 |
| 💽 | Жёсткие диски | 0.04 |
| 📱 | Телефоны | 0.045 |
| 💾 | ОЗУ | 0.07 |
| 🟡 | Разъёмы | 0.09 |
| ⚪ | Серебряная проволока | 0.12 |
| 🧠 | Процессоры | 0.18 |
| 🟩 | Советская плата | 0.25 |
| 🔶 | Латунные детали | 0.3 |
| 🟠 | Золотые контакты | 0.35 |
| 🪙 | Золотые пины | 0.5 |
| 🟨 | Плата шифратора | 0.75 |
| ⬜ | Платиновая пластина | 1.5 |

## Шапка игры

Три зоны:
1. **Деньги + слитки** — деньги сверху, граммы снизу.
2. **Престиж** — уровень + очки.
3. **Амулет** — точка + название.

Кнопка настроек справа.

---

## DEV-КОНСОЛЬ (убрать перед релизом!)

**Открывается:** 5 быстрыми тапами по надписи ДРАГМЕТ.

**Внутри:** деньги, слитки, очки, 16 предметов.

**Не закрывается автоматически.**

**⚠️ ПЕРЕД РЕЛИЗОМ УДАЛИТЬ:** HTML-блок `devOverlay`, CSS от `.dev-overlay` до `.dev-close`, JS-функции `devTap`, `openDev`, `closeDev`, `renderDevItems`, `applyDev`, переменные `devTapCount` / `devTapTimer`, проверки в `handleDown` и `processTap`.

---

## БАГ-РЕПОРТ (внешний, от Клода)

Получен внешний баг-репорт на v0.9. Разбор по пунктам:

### ✅ Починено в Заходе 1

1. **Дубликат крафта при двух горнах** — исправлено, проверка `forgeCrafts` перед push.
2. **Единичные предметы не попадают в кислоту** — исправлено, остаточный принцип распределения.
3. **`s.lv.priceControl` мёртвое поле** — удалено из стейта и сейва.
4. **Непоследовательное форматирование** — все цены через `fmt()`.
5. **Нет отклика кнопки в кулдауне** — добавлен звук + вибро.

### ❌ Не починено (в работе)

6. **Подсветка найденного не работает.** Проблема глубже, чем казалось: `render()` пересобирает инвентарь каждый кадр, класс `.flash` не успевает отрисоваться. Предложено решение через глобальные переменные, автор не тестировал.

### ⏸️ В плане (Заход 2 — баланс)

7. **Плавающая цена `s.price` не влияет на продажу.** Дрейфует, но стоп-игра от констант. Надо решить: убрать или встроить.
8. **Автокоп игнорирует стамину** — не баг, так задумано.
9. **Престиж-прокачки выкупаются за 3-4 престижа.**
10. **Порог 50-го уровня недостижим** — решается в Заходе 2.

### ⏸️ В плане (техдолг, Заход 4)

11. **Рендер 60 раз в секунду** — нагрев, батарея.
12. **`attachSwipe` переподключается каждый кадр.**
13. **Миграции сейвов** — нет единого механизма.
14. **Повторы в коде.**

### ❌ Клод ошибся

15. **`bumpToast()` мёртвая** — правило `.toast.bump` в CSS есть.
16. **`tone()` игнорирует `vol`** — правда мёртвый, но не баг.

---

## ЧТО УЖЕ СДЕЛАНО (накопительно)

- Двойные слоты сохранения.
- dt-масштабирование таймеров.
- Разделение тапа и свайпа.
- Единый уровень громкости.
- Тон кислоты растёт с прогрессом.
- 10 случайных надписей на кнопке копания.
- Цвет тоста по тиру предмета.
- Стоп-игра доступна с самого начала.
- Автокопатели за кэш, замедлены в 3 раза.
- Скроллбары скрыты.
- Порог оффлайна 5 минут.
- «Чуйка на золото» работает.
- «Склад II» работает.
- Сброс сейвов (v2).
- Кузница с анимациями и bloom.
- 10 амулетов.
- Свайп-удаление амулетов.
- Кузница до престижа (картинка, блюр, замок).
- Кузница уменьшена (max-width 240px).
- Замочек в таббаре не съезжает.
- Bloom 0.12, мягкий.
- Оффлайн-модалка упрощена.
- Обрезка текста в инвентаре.
- Стрелка аккордеонов заметнее.
- Мини-игра — диапазон цен до тапа.
- «Широкая зона».
- «Два горна».
- Вкладка «Копать» без скролла.
- 16 предметов.
- Локация «Дата-центр».
- Шапка переделана на зоны.
- Светлячки подняты выше.
- Тост встаёт на строку «N/M».
- Престиж: порог `100k × 1.5^N`, потолок 50.
- **Заход 1 багов:** дубликат крафта, единичные в кислоте, `priceControl`, `fmt()` в кнопках, отклик кулдауна.

---

## ПЛАН РАБОТ (актуальный)

### 🔴 Заход 1 — Критические баги

- ✅ Дубликат крафта
- ✅ Единичные предметы в кислоту
- ✅ `flashInvItem` по ключу (переход)
- ❌ Подсветка найденного — **НЕ РАБОТАЕТ**, нужен другой подход
- ✅ `s.lv.priceControl` мёртвое поле
- ✅ `tone()` мёртвый параметр (оставлен)
- ✅ Форматирование `fmt()` в кнопках
- ✅ Отклик кнопки в кулдауне

**Осталось:** починить подсветку. Решение через `window._flashInvKey` / `window._flashInvUntil` предложено, но автор не тестировал. **Начать со свежей головы — переосмыслить, почему `render()` каждый кадр ломает анимацию.**

### 🟠 Заход 2 — Баланс

Тут нужны решения автора:

1. **Плавающая цена `s.price`** — убрать / встроить в стоп-игру.
2. **Порог 50-го уровня** — оставить / понизить множитель / понизить потолок.
3. **Престиж-прокачки** — сделать конечными или дорожающими.
4. **Конечность всех прокачек** — финальные уровни у обычных и престижа.
5. **Очки престижа** — куда девать после 10-15 уровней.

### 🟡 Заход 3 — UX

Мелкие улучшения по ходу.

### 🟢 Заход 4 — Техдолг

- Разделить рендер на «дешёвый покадровый» и «полный по изменению».
- `attachSwipe` не переподключать каждый кадр.
- Миграции сейвов — единый механизм.
- Убрать повторы в коде.

### 🔵 Заход 5 — Пиксельный редизайн

Одним заходом: единый пиксельный шрифт, пиксельные иконки, жёсткие края.

### 🟣 Заход 6 — Релиз

- Убрать DEV-консоль.
- Обёртка в `.ipa` / `.apk`.

---

## Быстрый старт нового чата

> Игра ДРАГМЕТ, идл про аффинаж золота из электроники. Один `index.html` + `kuznec.png` + `kuznec-locked.png`, vanilla JS, без зависимостей. Открывается на iPhone через GitHub Pages как PWA.
>
> **Правила:** не писать код без явной команды «пиши» или «делай». Только полные файлы, не куски. Автор не программист — объяснять просто. Без воды. Перед задачей — уточнить, резюмировать, дождаться «делай». Большие задачи разбивать на этапы. Если фикс не работает — не просить автора «заменить две функции», только целый файл.
>
> **Текущий этап:** Заход 1, остался баг с подсветкой найденного. Дальше — Заход 2 (баланс).
>
> README с полным описанием приложу следующим сообщением.

---

## Легенда терминов

- **Сейв** — сохранение.
- **Слот** — один из двух файлов сохранения.
- **Престиж** — сброс прогресса за постоянные бонусы.
- **Автокопатель** — работяга на локацию.
- **Стамина** — запас точки.
- **dt** — время между кадрами.
- **PWA** — веб-приложение на домашнем экране.
- **WebView** — встроенный браузер внутри приложения.
- **Стоп-игра** — мини-игра с бегунком.
- **Bloom** — свечение от амулета.
- **Дев-консоль** — скрытая панель для тестов.
- **dvh** — динамический viewport height.
- **DOM** — «дерево» элементов страницы (что видит игрок).
- **innerHTML** — способ перерисовать блок с нуля.
- **Flash** — короткая подсветка элемента.

---

*Документ живой. Обновлять при изменениях.*
